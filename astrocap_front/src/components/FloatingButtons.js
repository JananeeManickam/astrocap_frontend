import React from "react";
import "./FloatingButtons.css"; // Import floating buttons CSS

const FloatingButtons = () => {
  return (
    <div className="floating-buttons">
      <button>Explore</button>
      <button>Ask</button>
      <button>Chat</button>
    </div>
  );
};

export default FloatingButtons;
